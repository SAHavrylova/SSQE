# Install for Python:
pip install pytest
pip install requests
pip3 install selenium --user
pip3 install -U webdriver_manager

# Formed Structure
conftest.py - file for save fixtures, precondition and postcondition
pytest.ini - markers fot tests 

# Command for test:
pytest -m check // "Start the test that involves checking markers"

